---
const baseUrl = new URL(Astro.site || '', Astro.url);
const isProduction = import.meta.env.PROD;
const basePath = isProduction ? '/GiftFromGod' : ''; // Use base path in production, empty in development
---
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/GiftFromGod"
            >GiftFromGod</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link active" href=`/GiftFromGod/taskforgod/`>Task For God</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
